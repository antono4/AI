interface:
  display_name: "AI"
  short_description: "Repo-specific patterns and workflows for AI"
  default_prompt: "Use the AI repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true